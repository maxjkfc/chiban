在 feat/unread-badges-ui 分支修復 unread state race issues.

### 變更內容
- provider 使用 revision/override state, 避免 in-flight GET /groups 在 mark-read 或 realtime message 後把 badge 復原.
- ChatRoom realtime message 更新 shared unread state; 在 reader 位於最新訊息時 best-effort 呼叫 mark-read; initial history merge 後標記實際 newest message, 而非只標記 REST page tail; 保留 sender 自己的訊息不增加 unread.
- 補 API contract, mark-read success, refresh failure fallback, stale GET/read response, realtime/own-message transition, new-group refresh race tests.
- ApiRequestError 改為明確欄位初始化, 保持行為不變, 讓現有 Node strip-only test runner 可載入 API module.

### 驗證結果
- 修正 realtime messages 在 room open/in-flight initial history 時 unread state 過期問題.
- 修正 in-flight GET /groups 在 mark-read 後復原 badge 的 race.
- tests: 10 npm tests passed, tsc/build/changed-files lint passed.
- full lint 的 pre-existing blocker: apps/web/lib/use-push.ts:28.

---
Review fixes (commit 8cb4781):
- Refined revision-safe refresh/read ordering.
- Added regression tests for new-group refresh race conditions.

---
Review fixes Round 2 (commit 7b04d0a):
Addressing blocking findings from t_ea331045 and t_47529ab6:
- Implemented window event bridge in UnreadProvider for global /ws/me message cross-group updates.
- Added per-group read request sequence to prevent cross-group response cancellation.
- Added revision guards to reject stale mark-read and groups refresh results.

Verification:
- npm test: 16/16 passed
- tsc: passed
- build: passed
